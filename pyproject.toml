[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "robot-1A-eibot-2026"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "opencv-python>=4.9.0",
  "numpy>=1.26.0",
  "pyserial>=3.5"
]
[tool.setuptools]
py-modules = []

